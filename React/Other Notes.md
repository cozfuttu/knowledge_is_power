* When naming files, always use kebap case (user-card.tsx).
* Use conventions like error.js, loading.js.
* When coding pages in the app directory, open up a route group called "(client)" and code the pages inside.